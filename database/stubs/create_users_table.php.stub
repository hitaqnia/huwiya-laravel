<?php

use App\Models\User;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('users', function (Blueprint $table) {
            $table->id();

            // Creates exactly the columns declared in $huwiyaFieldsMap on the
            // User model. `huwiya_id` (nullable unique ulid) is always added.
            // Supported claim keys: phone, email, name, locale, zoneinfo, theme.
            $table->huwiyaFields(User::huwiyaFieldsSchema());

            $table->rememberToken();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('users');
    }
};
